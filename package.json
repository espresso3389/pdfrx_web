{
  "name": "pdfrx-web",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "sync-assets": "node scripts/sync-assets.mjs",
    "build": "npm run build --workspace=@pdfrx/engine --workspace=@pdfrx/viewer-core --workspace=@pdfrx/viewer",
    "test": "npm run test --workspace=@pdfrx/viewer-core",
    "docs": "typedoc",
    "build:pages": "npm run build && npm run docs && npm run build --workspace=@pdfrx/example-basic && node scripts/assemble-pages.mjs",
    "dev": "npm run dev --workspace=@pdfrx/example-basic"
  },
  "license": "MIT",
  "devDependencies": {
    "typedoc": "^0.28.20"
  }
}
